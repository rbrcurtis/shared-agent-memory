{
  "version": 1,
  "hooks": {
    "stop": [
      {
        "command": "bash -c 'root=\"${CURSOR_PLUGIN_ROOT:-${PLUGIN_ROOT:-${CLAUDE_PLUGIN_ROOT:-}}}\"; test -n \"$root\"; exec \"$root/bin/shared-agent-memory\" --memory-turn-hook'"
      }
    ]
  }
}
