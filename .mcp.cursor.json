{
  "mcpServers": {
    "shared-memory": {
      "command": "bash",
      "args": [
        "-c",
        "root=\"${CURSOR_PLUGIN_ROOT:-${PLUGIN_ROOT:-${CLAUDE_PLUGIN_ROOT:-}}}\"; test -n \"$root\"; export DEFAULT_AGENT=\"${DEFAULT_AGENT:-cursor}\"; exec \"$root/bin/shared-agent-memory\""
      ]
    }
  }
}
